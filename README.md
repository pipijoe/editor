# Lexical + Vite + React + Tailwind + shadcn 编辑器

一个只保留 **Lexical 基础能力** 的编辑器示例：

- 基础富文本输入（段落）
- 文本样式：Bold / Italic / Underline
- 历史记录（撤销/重做快捷键）
- 使用 shadcn `Button` 替换原生按钮

## 启动

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```
